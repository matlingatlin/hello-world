{
  "name": "scio",
  "private": true,
  "version": "0.0.1",
  "description": "Scio — AI app builder monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@10.33.0"
}
