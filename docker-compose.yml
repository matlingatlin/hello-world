services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: scio
      POSTGRES_PASSWORD: scio
      POSTGRES_DB: scio
    ports:
      - "5432:5432"
    volumes:
      - scio-db:/var/lib/postgresql/data

volumes:
  scio-db:
