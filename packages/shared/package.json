{
  "name": "@scio/shared",
  "version": "0.0.1",
  "description": "Shared TypeScript types and DTOs for the Scio API contract",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo \"no tests yet\" && exit 0",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "class-validator": "^0.15.1"
  }
}
