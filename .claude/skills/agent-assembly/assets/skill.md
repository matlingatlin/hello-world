---
name: <directory name, hyphenated>
description: "<Use when … — triggering conditions and symptoms in the words a
  person types, third person, slightly pushy because skills under-trigger more
  often than they over-trigger. State what it decides, not how. End with NOT-clauses
  naming the neighbours that own adjacent jobs. Under 1024 characters.>"
---

# <Title — the job, not the topic>

<Two or three sentences: what this procedure is for, and the one idea holding it
together. If there is a measured reason the naive approach fails, put it here —
a rule that overrides what a competent practitioner naturally does, with the
mechanism, is the shape that discriminates.>

<Name the reference files this procedure opens, and when.>

## <N> · <Step name>

<What to do. Then the artefact.>

**Artefact:** <a number, a file:line, a table row, a named module, a verdict.
A step that ends in a consideration does not land.>

<Repeat per step. Each step names any file it opens.>

## When this does not apply

<The cases where running it produces noise, and which neighbour owns them.
A procedure that never declines is a procedure that cannot discriminate.>

<!-- TEMPLATE. Fields are empty on purpose: a filled-in exemplar is a
     near-domain stimulus and gets reproduced, choices included. Delete this
     comment and everything in angle brackets. -->
