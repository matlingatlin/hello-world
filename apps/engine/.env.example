# Copy to .env for local development. Never commit real secrets —
# in production these live in the Azure secrets manager (ADR-0004).

# Server
ENGINE_PORT=8000

# LLM providers (later kickoffs — matrix + multi-pass, extraction)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
