// Owned by pkg_foundation — the app shell.
export function SiteHeader() {
  return (
    <header className="header" data-scio-id="site-header" data-scio-package="pkg_foundation">
      Bistro Nord
    </header>
  );
}
